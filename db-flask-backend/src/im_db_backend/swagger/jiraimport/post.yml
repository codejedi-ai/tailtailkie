Authenticate POST endpoint authenticates current user
---
tags:
- jiraimport
parameters:
- in: body
  name: get the jira ticket
  schema:
    $ref: "#/definitions/JiraImportPOSTResponse"
  responses:
    200:
      description: User details associated with the token
      schema:
        $ref: "#/definitions/JiraImportPOSTResponse"