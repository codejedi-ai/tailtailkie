Token GET endpoint redirects to authorization code page
---
tags:
  - imdb
parameters:
- in: body
  name: IMDB authorization token
  schema:
    $ref: "#/definitions/IncidentPOSTRequest"
responses:
  200:
    description: Status of access token retrieal
