Authenticate POST endpoint authenticates current user
---
tags:
- authenticate
parameters:
- in: body
  name: User token
  schema:
    $ref: "#/definitions/AuthenticatePOSTRequest"
responses:
  200:
    description: User details associated with the token
    schema:
      $ref: "#/definitions/AuthenticatePOSTResponse"
