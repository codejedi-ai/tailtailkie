import "dotenv/config";
import { defineConfig } from "prisma/config";

export default defineConfig({
  schema: "prisma/schema.prisma",
  migrations: {
    path: "prisma/migrations",
  },
  engine: "classic",
  datasource: {
    url: "mongodb+srv://admin:G.D7pZRqC-zUcp9@docker-mlh.fyx6w.mongodb.net/tensorstore?retryWrites=true&w=majority&appName=docker-mlh",
  },
});
